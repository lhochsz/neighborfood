<!DOCTYPE html>
<html lang="en">
<head>
  <% include ../partials/head %>

  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.12.4/jquery.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
</head>

<body class="container-fluid">
  <header>
    <% include ../partials/header %>
  </header>

  <main>
      <div class="jumbotron index">
        <h3>MyFridge</h3>
      </div>

      <a href="/fridge/new" class="btn btn-success" class="new">Add New Item</a>
      <div class="indexExplain">
        <br>
        <p>Add your leftovers or extra food items below to share with your community.  Then head to the Neighborhood tab to see what your neighbors are offering!</p>
      </div>

    <h4 class="fridgeIndexCaptialize">
          <% fridgeItems.forEach(function(fridgeItem) { %>
          <form method="POST" action="/fridge/<%= fridgeItem._id %>?_method=DELETE">

          <div class="container">
            <div class="panel-group">
              <div class="panel panel-default">
                <div class="panel-heading">
                  <h4 class="panel-title">
                    <a data-toggle="collapse" href="#<%= fridgeItem.id %>" class="fridgeFood"><%= fridgeItem.food %></a>
                    <p class="updated"> Updated: <%= fridgeItem.getUpdatedAt() %></p>
                      <ul>
                        <li class="dropdownli"><button class="btn btn-xs btn-info"><a href="/fridge/<%= fridgeItem._id %>">Show</a></button></li>
                        <li class="dropdownli"><button class="btn btn-xs btn-info"><a href="/fridge/<%= fridgeItem._id %>/edit">Edit</a></button></li>
                        <li class="dropdownli"><button type="submit" class="btn btn-xs btn-danger">Delete</button></li>
                      </ul>

                  </h4>
                </div>
                <div id="<%= fridgeItem.id %>" class="panel-collapse collapse">
                  <div class="panel-body"><%= fridgeItem.food + ' - ' + fridgeItem.amount + ' - ' + fridgeItem.neighborhood %></div>
                  <% if (fridgeItem.ingredients) { %>
                    <p class="indexIngredients"> Ingredients: <%= fridgeItem.ingredients %> </p>
                  <% } %>
                   <p class="image"><img src="<%= fridgeItem.photo %>"><p>
                </div>
              </div>
            </div>
          </div>
        <% }); %>
          </form>
      </h4>




  </main>

  <footer>
    <% include ../partials/footer %>
  </footer>
</body>
</html>
